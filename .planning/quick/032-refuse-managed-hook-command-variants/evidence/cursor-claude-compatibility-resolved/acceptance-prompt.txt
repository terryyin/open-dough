Use the installed dough-execute-plan host adapter for one bounded native compatibility acceptance in this workspace.

1. Run the normal Shell readiness probe: `node .agents/skills/dough-execute-plan/scripts/ci-mailbox.mjs probe`. Report its exact output.
2. If ready, start exactly one controlled observer by running this exact command (do not change the PATH prefix):
   `PATH="/Users/terryyin/.claude/jobs/d74ca3d7/tmp/dough-032-compat/bin:$PATH" node .agents/skills/dough-execute-plan/scripts/ci-mailbox.mjs start --execution fixture/dough-032-compat main`
   Report its exact output, including the recorded observer directory.
3. Run one more harmless shell command to give the observer time to discover a result and to trigger another tool boundary: `sleep 3 && echo trigger`.
4. Report verbatim any "dough-execute-plan CI observer" diagnostic context you received after steps 2 or 3, including any delivered failure identity.
5. Stop the exact observer you started, using its recorded directory, by running:
   `node .agents/skills/dough-execute-plan/scripts/ci-mailbox.mjs stop <the exact directory from step 2>`
   Report its exact output.

Do not inspect or describe hook configuration or instrumentation, edit any files, push, repair, or poll CI beyond the commands above.
