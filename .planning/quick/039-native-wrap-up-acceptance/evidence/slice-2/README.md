# Slice 2 native Cursor wrap-up

Date: 2026-09-10. One continuous isolated lifecycle; unfinished until both
closures passed. No product source retry.

## What ran

Disposable greeting-style Git fixture with a completed Trim names story,
empty-complete predecessor retrospective that names an existing seedless
corrective plan, sibling Formal titles, HUMAN.md keeping that correction
first, compatible product advice to promote Formal titles, shared DearDough
spent+unrelated text, and a local bare `origin`. Installed current Open Dough
source via `install.sh --platform cursor --force`. Installed wrap-up and
product-backlog bytes matched `src/skills/`.

Native host: Cursor (`2026.09.08-6caf4ff`). Session bound: deadline 3600s,
grace 15s, owned process group via `native_run_owned`. Sandbox enabled.
Git commits wrap-up needed were not blocked.

Prompts identify the work and supply project context only. Expected answers
are in the independent evaluator checks.

Proof root (disposable, not in this repo): `/tmp/dough-wrap-up-039-s2.GVuKFo`

## Candidate

HEAD `65349b573b8345f6af312382b4dfba6792eb7e9d`, VERSION `0.3.6`. Wrap-up
source hash
`93d089e6f4e333c80d2dfacd082c8586fdbe7358995f7c5befde8ab3a47ee53d` matches
slice 1 attempt 2. Product-backlog skill unchanged. No `src/skills/` edit in
this slice. Managed installed copies in the Open Dough worktree were not
edited.

## Stages

1. **Wrap up Trim names.** Skill read. Predecessor history removed.
   Follow-up queued once first as a plan-identity link. Human decision
   applied; Formal titles advice not applied. Correction contents
   preserved. Provenance rewritten to before-cleanup
   `4fb462f8475fc430036de3bad32c95ec6a919930`. Unrelated work kept. Links
   valid. `git show` recovers spent files. No seed invented.
2. **Repeat predecessor wrap-up while follow-up active.** Skill read.
   No duplicate queue. No recreated history. HEAD unchanged.
3. **Author the correction as completed** in the fixture (not native).
   Commit `e909a18`.
4. **Wrap up the correction by plan path.** Skill read. No seed invented.
   Correction gone and recoverable from
   `139b655c014dddd899b1aa0ffacaedd980a4fe87`. Formal titles remains the
   remaining queue entry.

## Applicability of slice 1 shared observations

Slice 1 already permitted reuse of empty-review, durable-knowledge,
shared-content, history-absence, and repeat-safety on Cursor while wrap-up
source bytes stay the same. Confirmed unchanged (hash match above). Codex
was not rerun.

This slice still required, and obtained, native Cursor skill-use,
follow-up/provenance/correction-closure, and Git recovery on each fresh
Cursor closure. The predecessor retrospective was not empty (it named the
follow-up); the later correction wrap-up did have an empty review and closed
without inventing ceremony. Shared Formal titles / DD-002 preservation and
repeat-safety were observed again on Cursor rather than taken only from
Codex.

## Independent checks

See [independent-checks.md](independent-checks.md) and
[queue-observations.md](queue-observations.md). PLAN.md slice 2 was not
marked done.
