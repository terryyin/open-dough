# Slice 2 behavior walkthrough

Candidate: same worktree skill after the shared direction-alignment edit.
Date: 2026-09-10. Inspected rationale and destinations, not repeated labels.

## Digression case

**Input.** Fixture `evidence/slice-2/digression/`: crash-fix contract plus an
unrequested dashboard widget; transcript of a repeated docs reread and an
excluded forecast-library investigation. Direction: reliable billing export.

**Routing.**
- Implementation: the widget is outside the original contract. That is a
  bounded contract defect for correction planning (remove or get a scope
  decision). Not a direction rewrite.
- Process: the second docs reread after the cause was known, and the
  forecast-library detour, are avoidable digression. Process recommendations
  only; not added to the correction plan.
- Product: no backlog recommendation for dashboard or charts; they do not
  advance the current export direction.

**Destinations.** Direction file unchanged. Product suggestions not placed in a
correction plan. No dashboard/chart queue edits.

## Later-changed-direction variation

**Input.** Fixture `evidence/slice-2/later-direction/`: approved hook
registration under a prior install/update direction; today's direction is
extract remaining planning skills.

**Result.** The completed work is not a defect. It matched its original
approved contract. Today's direction applies to remaining and proposed work
(queued refinement extraction), not to relabeling the historical delivery.

**Destinations.** Direction text preserved. No correction plan for "wrong
direction."
