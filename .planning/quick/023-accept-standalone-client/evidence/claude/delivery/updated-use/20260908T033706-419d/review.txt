Review 2026-09-08 of Claude Code delivery/updated-use attempt
20260908T033706-419d. Runs the retain-on-abort fix applied to
tests/support/dough-adr-awareness-updated-use.sh and
tests/support/dough-adr-awareness-release-transition.sh (see the
sibling fix proof at
tests/native-delivery-updated-use-adapters.sh). Command:
  bash tests/dough-adr-awareness-claude-delivery-to-use.sh --native \
    --case delivery/updated-use \
    --results-dir .planning/quick/023-accept-standalone-client/evidence \
    --deadline 300 --grace 15
Wrapper exit 1. Native version 2.1.263 (Claude Code), adapter
claude-stream-json. Deadline/grace were not hit (execution-status:
completed, i.e. the claude process exited 0; the failure is the
payload compare that runs after it).

Retention worked this time: result-path, record, observations.txt,
update-events.jsonl, update-response.md, update-stderr.log, both
update snapshots, and both source snapshots are all present. Nothing
was deleted by an EXIT trap. This is the fix's proof: an unfixed
harness would have hit the same cmp failure below and lost all of
this to the scratch-delete trap installed by delivery_prepare_fixture,
exactly as it did in the original attempt 20260908T031111-5393.

What happened in the session (from update-events.jsonl):
1. Claude invoked $dough-update (Skill tool), matching the prompt.
2. It read the installed dough-update SOURCE
   (file:///.../fixture-source) and VERSION (0.2.1), and globbed the
   installed .claude/skills tree and the recorded source tree. This
   is the correct start of the documented ordinary-update procedure
   (src/skills/dough-update/SKILL.md step: "Run `git ls-remote --tags`
   ... Keep vMAJOR.MINOR.PATCH").
3. It ran `git ls-remote --tags -- file:///.../fixture-source` to pin
   the latest release tag. This Bash call was denied three times
   (system subtype permission_denied, tool_name Bash, decision_reason
   "This command requires approval"), because the selected-journey
   launch command
   (tests/support/native-run-supervise.sh native_run_context_command,
   claude branch) passes `--allowedTools 'Read,Glob,Grep,Skill'` with
   `--permission-mode default` and no `--dangerously-skip-permissions`.
   Bash is not pre-allowed, and in `--print` (headless) mode a
   permission prompt cannot be answered, so it is denied outright.
4. Claude stopped and reported: "This command needs your approval to
   run (it reads the recorded Open Dough source outside the project
   directory). Please approve the `git ls-remote --tags` command..."
   No file was written. update-version-after stays 0.2.1 (unchanged),
   improvement-after-update: false, real-transition: false
   (observations.txt).
5. delivery_assert_update_payload then compared the (unchanged,
   shorter, pre-update) installed dough-adr-awareness/SKILL.md against
   the newly published v0.2.2 fixture-source SKILL.md (which has the
   appended "Improved conflict evidence" section) and failed with the
   same symptom as before: `cmp: EOF on .../SKILL.md`.

Review judgment on the three possibilities named for this task:

- Lost evidence from abort-before-retain: NO, for this attempt -- the
  fix retained everything. It WAS the reason the original attempt
  20260908T031111-5393 had nothing to review; that defect is now
  independently fixed and proven by
  tests/native-delivery-updated-use-adapters.sh's new payload-abort
  case (NATIVE_AGENT_TRUNCATE_INSTALLED_SKILL=1), without a native
  session.
- Claude not completing ordinary update: YES. This retained record
  shows Claude correctly started dough-update's real procedure (read
  SOURCE/VERSION, discovered installed skills, attempted the
  documented `git ls-remote --tags`) and was blocked purely by the
  selected-journey harness's own tool-permission scope
  (--allowedTools omits Bash under --permission-mode default, no
  --dangerously-skip-permissions, no interactive approval possible in
  --print mode). The candidate was never actually exercised.
- Product defect in 6682816: NOT demonstrated. dough-update's
  documented step of running `git ls-remote --tags` is the candidate's
  intended contract, not a defect; Claude asking to run it is correct
  behavior for an unprivileged session. No evidence here implicates
  6682816's own logic. dough-update is left unchanged.

Claude verdict: pending. Execution completed (no timeout, no crash),
but the harness's own launcher configuration for this selected case
prevented Claude from ever attempting the real update, so no update
or fresh-use behavior was exercised. This is a harness-configuration
gap (`--allowedTools` too narrow for dough-update's own contract),
analogous in kind to Codex's separate pending harness/stream issue --
not a demonstrated pass, not a demonstrated candidate failure, and not
lost evidence. Fresh-session installed load, ADR use with source
unavailable, and preservation of unrelated guidance/other roots remain
unrun; use-execution: unrun, use-pending: true (observations.txt).

Recommendation (not actioned here, out of this task's scope): the
selected Claude Code launch command in
tests/support/native-run-supervise.sh needs `Bash` added to
`--allowedTools` (or an equivalent permission grant) before a native
`delivery/updated-use` run can ever get past dough-update's own
`git ls-remote` step. That is a harness change, not a dough-update or
candidate-6682816 change, and needs its own acceptance proof; it is
not made in this diagnostic pass.

6682816 status: unchanged. No product or runner change was needed or
made beyond the two retain-on-abort files named above. Do not name a
new candidate. Do not re-run native for this leaf until the harness
permission gap above is addressed by its own scoped change.
