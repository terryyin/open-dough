# Slice 1 behavior walkthrough

Candidate: Open Dough `dough-execution-retrospective` at worktree HEAD after the
Slice 1 skill edit. Reviewer: executing-agent walkthrough of the updated
`src/skills/dough-execution-retrospective/SKILL.md`. Date: 2026-09-10.
Authority: recommendations only; no backlog writes. No native host re-run.

## Quick 031 — real product-learning case

**Input.** Request: review the completed CI host-hook registration execution.
Recovered plan: `.planning/quick/031-register-ci-host-hooks/PLAN.md`. Story:
SEED-001 Story 8, complete in `v0.3.4`. Direction from
`.planning/PRODUCT-BACKLOG.md`: extract Donut skills until Open Dough covers the
full software development lifecycle.

**Read.** Direction, the 031 plan source/scope and completion evidence, Story 8
status, and Recently done. Inspected SEED-007's already-queued updater
acceptance only because 031's own seed names that outstanding native proof as
related leftover work. Did not read unread queue entries such as SEED-006.

**Product result.** Grounded no-change. The delivered install/update
registration advances the established direction. No simpler replacement outcome
was evidenced. Conflict-refusal follow-up (Quick 032) is an implementation
correction, not a new product story. Remaining Claude updater acceptance is
already queued; this review does not add a duplicate or claim unread items are
validated.

**Destinations.** No backlog, seed, or direction file edited. Product
suggestion not placed in a correction plan.

**Limitations.** No new implementation audit. Process conclusions are not claimed
from commit count. Direction text was not proposed or changed.

## Urgent-fix variation

**Input.** Fixture `evidence/slice-1/urgent-fix/`: completed "Stop the nightly
export crash" against direction "Ship a reliable billing export for finance
operators." Unrelated queued stories exist in `seeds/SEED-A.md`.

**Read.** Fixture direction, SEED-B, and the completed plan. Did not open
`seeds/SEED-A.md` during the review walk (it exists only as unused queue bait).

**Product result.** Justified exception: an urgent crash fix that still serves
the billing-export direction. No queue recommendation.

**Destinations.** Fixture files unchanged. No direction edit.

## Missing-context variation

**Input.** Fixture `evidence/slice-1/missing-context/` contains only
`plans/001-rename-button.md`. No direction or backlog conventions.

**Product result.** Alignment cannot be assessed against an established
direction. Product conclusions remain provisional: backlog/story conventions are
missing and were not invented. Independently supported implementation review of
the recovered plan can still proceed.

**Destinations.** No files created to fill the gap.

## Hypothesis variation

**Input.** Fixture `evidence/slice-1/hypothesis/`: export work inspired a
customer-facing CSV download without a named beneficiary or evaluable outcome.

**Product result.** Labeled hypothesis. Concrete exploration proposal: identify
beneficiary and evaluable outcome before any queue addition. Discovery or
decomposition was not launched.

**Destinations.** Fixture backlog unchanged.
